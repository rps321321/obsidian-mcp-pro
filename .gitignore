node_modules/
build/
.env
.env.local
*.tgz
.DS_Store

# MCP registry credentials — never commit
.mcpregistry_*_token

# Local publisher tooling and test artifacts
mcp-publisher.exe
mcp-publisher.tar.gz
.playwright-mcp/

# Claude Code per-machine settings (permissions, allowlists). Shared
# project config should live in `.claude/settings.json` (no `.local`),
# which stays tracked so collaborators share the same rules.
.claude/settings.local.json
